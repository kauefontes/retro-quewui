// Re-export types and API methods

// Re-export types
export type { Post } from '../../../types/index';

// Re-export API methods
export { createPost, updatePost, deletePost } from '../../../data/api';
