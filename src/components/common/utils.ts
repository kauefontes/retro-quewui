// Re-export types and authentication utilities

// Re-export types
export type { User } from '../../types/index';

// Re-export authentication context
export { useAuth } from '../../contexts/AuthUtils';
